apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-certificate
spec:
  secretName: wildcard-certificate
  dnsNames:
    - "*.ukgovcamp.com"
  issuerRef:
    name: issuer
    kind: ClusterIssuer
