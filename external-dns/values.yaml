txtOwnerId: external-dns
txtPrefix: external-dns.
domainFilters:
- ukgovcamp.com
sources:
- gateway-httproute
- service
provider:
  name: cloudflare
env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-api-token
        key: api-token
